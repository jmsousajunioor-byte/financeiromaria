-- schema.sql
-- Dump of full schema for glow-fin-app (same as migration)

-- Extensions
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";
CREATE EXTENSION IF NOT EXISTS pgcrypto;

-- (Functions, tables, indexes, triggers, policies as in migrations/0001_full_schema.sql)
-- For convenience, the migration file supabase/migrations/0001_full_schema.sql contains the full DDL.
